{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/output",
            "args": ["test.pdf", "1", "hi"],
            "cwd": "${workspaceFolder}"
        }
    ]
}